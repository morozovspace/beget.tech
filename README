Для запуска проекта вам потребуется установить:
SASS: https://sass-lang.com/install

Компиляция стилей запускается командой: (необходимо установленный SASS)
$ sass style.scss style.css

Для разработки и генерации изменений:
$ sass --watch style.scss:style.css --style compressed
